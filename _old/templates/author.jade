
mixin author(authorName)
  - var author = contents.authors ? contents.authors[authorName + '.json'] : null
  span.author
    if author
      a(href='mailto:'+author.metadata.email)= author.metadata.name
    else
      =authorName
